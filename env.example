# Database Configuration
DB_HOST=localhost
DB_PORT=3307
DB_USER=root
DB_PASSWORD=Karosa0797!
DB_NAME=debt_manager

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_here
JWT_EXPIRES_IN=24h

# Email Configuration (Gmail SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Twilio Configuration (for SMS and Voice)
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+254112851330

# Webhook Configuration
WEBHOOK_URL=https://your-domain.com

# Phone Service Configuration
PHONE_SERVICE_ENABLED=true # Enable/disable phone service
PHONE_MOCK_MODE=true # Set to false when using real Twilio
PHONE_DEFAULT_VOICE=alice # Voice for calls (alice, man, woman)
PHONE_DEFAULT_LANGUAGE=en-US # Language for calls (en-US, en-GB, es-ES)
PHONE_MAX_DURATION=300 # Max call duration in seconds (5 minutes)
PHONE_RETRY_ATTEMPTS=3 # Number of retry attempts for failed calls

# Server Configuration
PORT=5000
NODE_ENV=development

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info
LOG_FILE=./logs/app.log

# CORS
CORS_ORIGIN=*
